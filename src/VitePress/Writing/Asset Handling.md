# Asset Handling

## The Public Directory
Assets placed in public will be copied to the root of the output directory as-is.
>可由资源目录改变  
>默认/public

## Base URL
主配置文件根下配置base：https://foo.github.io/bar/, then base should be set to '/bar/' (it should always start and end with a slash).